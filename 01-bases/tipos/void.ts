(() => {

    // Void: Indica que una función no retorna ningún valor. 
    // Es decir, su propósito es ejecutar código pero no devolver nada.
    function callPhone(): void {
        
    }

    const callPhone2 = (): void => {

    }

    const a = callPhone();
    console.log(a);
})();
